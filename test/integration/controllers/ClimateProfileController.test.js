'use strict'
/* global describe, it */

const assert = require('assert')

describe('ClimateprofileController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['ClimateprofileController'])
  })
})
