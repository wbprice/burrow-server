'use strict'
/* global describe, it */

const assert = require('assert')

describe('ClimateProfile Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ClimateProfile'])
  })
})
